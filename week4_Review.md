# Week 4 Review - Dashboard, KPI, E2E Coverage

วันที่ตรวจ: 5 กันยายน 2569

## สถานะโดยรวม

งานสัปดาห์ที่ 4 อยู่ในสถานะพร้อมทดสอบบนเครื่องและพร้อมนำ API ขึ้น Supabase เมื่อ connector กลับมาใช้งานได้อีกครั้ง ส่วน frontend ยังไม่ได้ build หรือ deploy ตามที่ผู้ใช้ระบุว่าจะ deploy เอง

## สูตร KPI ตามเอกสาร

อ้างอิงจาก `documents/ISRI_บทที่1-2_ฉบับปรับแก้.docx` และ `documents/ISRI_บทที่3_วิธีการดำเนินงาน.docx`

- อัตราการตอบรับภายใน SLA = จำนวนรายการที่ตอบรับภายใน SLA / จำนวนรายการที่มีการตอบรับทั้งหมด x 100
- อัตราการปิดงานภายใน SLA = จำนวนรายการที่ปิดงานภายใน SLA / จำนวนรายการที่ปิดงานทั้งหมด x 100
- เวลาเฉลี่ยในการตอบรับงาน = ผลรวมเวลาตอบรับงานของทุกรายการ / จำนวนรายการที่มีการตอบรับ
- เวลาเฉลี่ยในการปิดงาน = ผลรวมเวลาที่ใช้ในการปิดงานของทุกรายการ / จำนวนรายการที่ปิดงาน

รายละเอียดการจับเวลาในระบบ:

- เวลาเฉลี่ยตอบรับ เริ่มจากเวลาผู้แจ้งส่งรายการ ถึง event แรกที่ช่างตอบรับงาน
- เวลาเฉลี่ยปิดงาน เริ่มจากเวลาสร้างใบงานซ่อมบำรุง ถึง event ปิดงานสำเร็จ
- SLA deadline ของการตอบรับและปิดงานยังใช้ `respond_due_at` และ `resolve_due_at` ที่คำนวณจากเวลารับแจ้งตามบทที่ 3 เพื่อให้เวลารอจัดสรรถูกนับใน SLA
- ตัวหารของอัตรา SLA ใช้เฉพาะรายการที่มีการตอบรับหรือปิดงานในเดือนที่เลือก งานที่ยังไม่ถึงขั้นตอนนั้นไม่ถูกนำเข้าตัวหาร

## สิ่งที่พัฒนาในรอบนี้

- เพิ่ม helper คำนวณ KPI ให้แยกนิยามเวลาเริ่มต้นของ Response Time และ Average Closure Time ตามเอกสาร
- แก้การนับ KPI ไม่ให้ event หลุดข้ามเดือน และไม่ให้งาน rework ทำให้การตอบรับครั้งหลังถูกนับแทนครั้งแรก
- เพิ่ม dashboard workload ของช่างให้เห็นงานหลัก งานร่วม งานค้าง งานเกิน SLA งานรอตรวจ/อนุมัติ และ PM ที่รับผิดชอบ
- เพิ่ม hotspot grouping ตามอาคาร ชั้น พื้นที่ และชิ้นงาน โดยไม่นับรายการที่ถูกปฏิเสธ
- เพิ่ม summary PM บนแดชบอร์ด ได้แก่ เกินกำหนด ใกล้ครบกำหนด ยังไม่มอบหมาย และผล PM ในเดือนที่เลือก พร้อมลิงก์ไปประวัติ
- ปรับข้อความ UI ของ KPI ให้ตรงกับสูตรในเอกสาร และลดความกำกวมเรื่องตัวหาร

## ผลทดสอบ

- `deno test --allow-env=WEB_ORIGIN api/tests` ผ่าน 38 tests
- `deno check api/supabase/functions/isri-api/index.ts` ผ่าน
- `tsc --noEmit -p tsconfig.app.json` ผ่าน
- `oxlint` ผ่าน
- `node api/tests/plan3_database.mjs` ผ่าน 9 checks สำหรับคะแนน รางวัล การคืนคะแนน PM และสิทธิ์ RPC
- ตรวจ UI fixture บน localhost แล้ว: desktop/mobile แสดงสูตร KPI ใหม่, hotspot grouping, workload และ PM summary โดยไม่มี console error และไม่มี horizontal overflow บน viewport กว้าง 390px

## ข้อจำกัดที่ยังเหลือ

- Supabase connector ตอบกลับ `token_expired` ระหว่างตรวจ จึงยังไม่ได้ deploy Edge Function รอบ Week 4 ขึ้น Supabase
- Netlify credit หมดตามที่ผู้ใช้แจ้ง จึงยังไม่ได้ทดสอบ frontend ที่ deploy จริง
- การทดสอบ E2E ผ่านเว็บจริงครบทุก role ต้องทำหลังมี frontend deploy หรือเปิด localhost พร้อมบัญชีทดสอบที่ login ได้

## สรุปเทียบแผนสัปดาห์ 4

- Dashboard จำนวนการแจ้งรายเดือนและสัดส่วนสถานะงาน: ครบในโค้ดและทดสอบสูตรเดือน
- KPI 4 รายการ: ครบและปรับตามเอกสาร
- ภาระงานช่างและจุดพบปัญหาซ้ำ: ครบใน dashboard summary และ UI
- ทดสอบสิทธิ์/สถานะ/อะไหล่/ตรวจรับ/คะแนน/คะแนนไม่พอ/รางวัลหมด: ครอบคลุมด้วย unit และ local DB integration
- E2E แจ้งเหตุถึงแลกรางวัล: logic และ DB path ครอบคลุมด้วย local tests แต่ยังไม่ได้ยืนยันผ่านเว็บ deploy จริง
- PM สร้างแผน ช่างบันทึกผล และสะท้อนแดชบอร์ด: ครอบคลุมด้วย service/repository tests และ UI fixture
